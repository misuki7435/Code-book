#include<bits/stdc++.h>
#define _for(i,a,b) for(int i=(a);i<(b);i++)
#define R(X) cin>>x
#define _vec(X) vec.at(X)
#define PB push_back

using namespace std;
inline void R_P(vector<int> &vec){
	int temp;
	cin>>temp;
	vec.push_back(temp);
} 
