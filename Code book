#include<bits/stdc++.h>
#define _for(i, a, b) for (int i = (a); i < (b); i++)
#define PB emplace_back
#define MINI -2147483647
#define MAXI 2147483647
#define MAXLL 9223372036854775807
#define MINLL -9223372036854775807
#define VI vector<int>
#define VB vector<bool>
#define VLL vector<long long>
#define LL long long
#define S size()
#define ALL(X) X.begin(), X.end() 
//#define Miku_My_Wife
 
using namespace std;
inline void R_P(vector<int> &vec) {
  int temp;
  cin>>temp;
  vec.push_back(temp);
} 
inline bool checkMax(int &base, int test) {
  return (test > base) ? base = test, true : false;
}
template <class T>
inline bool checkMin(T &base, T test){
  return (test < base)? base = test, true : false;
}
template <class T>
void test(vector<T> &vec, const string &warn) {
  cout<<warn<<'\n';
  _for(i, 0, vec.size())
    cout<<vec[i]<<' ';
  cout<<"\nTest end..............\n";
}
template<class T>
void test2d(vector<vector<T> > &vec, const string &warn) {
  cout<<warn<<'\n';
  _for(row, 0, vec.S) {
    _for(col, 0, vec[row].S)
      cout<<vec[row][col]<<' ';
    cout<<'\n';
  }
  cout<<"\nTest end..............\n";
}

int main () {
  #ifdef Miku_My_Wife
  freopen("input.txt", "r", stdin);
  freopen("output.txt", "w", stdout);
  #endif



  return 0;
}
